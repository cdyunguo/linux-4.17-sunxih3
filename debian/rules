#!/usr/bin/make -f

build:
	$(MAKE) KERNELRELEASE=4.17.14-sunxi ARCH=arm KBUILD_SRC=

binary-arch:
	$(MAKE) KERNELRELEASE=4.17.14-sunxi ARCH=arm KBUILD_SRC= intdeb-pkg

clean:
	rm -rf debian/*tmp debian/files
	$(MAKE) clean

binary: binary-arch
